## 커밋 컨벤션

형식: `[SESAC-번호] <type>(<scope>): <제목>`

예시:

    [SESAC-123] feat(core): 버스 노선 최단경로 탐색 추가

    기존 선형 탐색이 환승 2회 이상에서 느렸음.
    노선을 인접 리스트로 전처리 후 다익스트라 적용.

### 규칙
- 티켓 ID는 `workbench/linear-ticket.md`의 대장이나 사용자가 지정한 값을 우선하고,
  없으면 브랜치명에서 추출한다.
  `feature/sesac-123-fix-login` → `SESAC-123`
- 둘 다 없으면 커밋하지 않는다. 커밋을 어떻게 나눌지 먼저 제안하고
  대장에 티켓 상세를 작성한 뒤, Linear 이슈를 만들어 번호를 알려달라고 요청한다.
  사용자가 티켓 없이 진행하라고 한 경우에만 `[...]` 부분을 생략한다.
- 한 브랜치에서 여러 티켓의 커밋을 만들 수 있다.
  이때는 커밋마다 해당 티켓 ID를 쓴다.
- type: feat | fix | refactor | perf | data | exp | test | docs | chore
- scope: core | data | scripts | workbench | config
- 제목: 50자 이내, 한국어, 마침표 없음
- 본문에는 "무엇"이 아니라 "왜"를 쓴다.
- 도구 서명이나 Co-Authored-By 트레일러를 넣지 않는다.

## 저장소 범위

- `workbench/`는 의도적으로 Git에서 제외한다. 추적을 제안하지 않는다.
  분석 보고서, 인계 문서, 티켓 대장 같은 작업용 문서가 여기 있다.
- `data/`, `output/`, `.cache/`도 재생성 가능하므로 제외한다.
- 제외된 경로의 파일은 커밋 대상 목록에 넣지 않는다.

## 티켓 대장

`workbench/linear-ticket.md`에 날짜별로 티켓과 커밋을 1:1로 기록한다.
사용자는 Linear에서 이슈를 만들고 번호만 채우므로, 붙여넣을 내용은 미리 작성해 둔다.

이 대장은 Git에서 제외된 로컬 문서다. 없으면 이 규칙대로 새로 만든다.

### 규칙
- 커밋 전에 오늘 날짜 섹션의 표에 행을 추가한다.
  열은 `# / 티켓번호 / 제목 / 내용 / 커밋 파일 / 커밋 메시지 / 상태`다.
- 티켓번호는 `SESAC-` 까지만 쓰고 비워 둔다. 번호는 사용자가 채운다.
- 문서 하단 "티켓 상세"에 `배경` / `작업 내용` / `범위 밖`을 작성한다.
  이것이 Linear 이슈 설명란에 그대로 들어간다.
- 새 날짜 섹션은 문서 가장 위에 추가한다. 과거 섹션은 수정하지 않는다.
- 티켓도 커밋도 정하지 못한 항목은 그 날짜의 "미배정" 표에 남긴다.
- 커밋한 뒤 상태를 `☐`에서 `☑`로 바꾼다.
  대장은 Git에서 제외되므로 이 갱신은 커밋 대상이 아니다.
