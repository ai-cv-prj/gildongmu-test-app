# file_path: requirements.txt
# 서버·테스트·모델 추론 패키지 목록

# 공통
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
python-dotenv>=1.0
opencv-python-headless>=4.9
numpy>=1.26
pydantic>=2.6

# 테스트
pytest>=8
httpx>=0.27

# 모델 추론
# GPU 사용 시 아래 명령으로 PyTorch를 먼저 설치한 뒤 이 파일을 설치한다.
# WSL / CUDA 13.0:
# python -m pip install torch==2.14.0 torchvision==0.29.0 --index-url https://download.pytorch.org/whl/cu130
torch==2.14.0
torchvision==0.29.0
ultralytics==8.4.152

# 보행 추론 (Python 3.12 이상)
# Python 3.14에서는 해당 wheel이 있는 lap 0.5.13 사용
lap==0.5.12; python_version < "3.14"
lap==0.5.13; python_version >= "3.14"
transformers==5.17.0
scipy==1.18.1
PyYAML==6.0.3
Pillow==12.3.0
imageio-ffmpeg==0.6.0
